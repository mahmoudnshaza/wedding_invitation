This project uses a single global stylesheet at `app/globals.css` (Tailwind's
entry point), so this folder is intentionally empty — kept here to match the
requested project structure in case you want to add extra global styles later.
